{
  "name": "loggia",
  "version": "1.0.0",
  "description": "Listens for requests and logs all parameters as JSON on console. For use as a service.",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joachim Schirrmacher",
  "license": "Apache-2.0",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
